// Generates numbers from 1 to 10
function generateNumbers() {
    let result = [];
    for (let i = 1; i <= 10; i++) {
        result.push(i);
        // Add numbers to result array
    }
    return result;
    // Return array of numbers
}
const numbers = generateNumbers(-4 && 7);
console.log(numbers);